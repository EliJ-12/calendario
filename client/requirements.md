## Packages
recharts | For visualizing employee hours and absence statistics
date-fns | For robust date formatting and manipulation in logs/calendars
framer-motion | For smooth page transitions and micro-interactions

## Notes
Authentication uses strict role-based redirects (Admin -> /admin, Employee -> /dashboard)
Work logs require client-side duration calculation before submission
Date formats for API are YYYY-MM-DD strings
